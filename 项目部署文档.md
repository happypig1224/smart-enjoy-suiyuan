## 前置工作

1、服务器---》相当于一台电脑，这是部署项目必须的，购买服务器会赠送公网IP

注意：自己购买的电脑是局域网IP，不是公网IP。

2、域名---》需要备案才能使用

3、选择支持FTP文件协议的终端应用---》手拖拽传输文件XSHEEL、XFTP、FINALSHELL

4、安装所需的软件，如Docker、JDK、Python、Nodejs等

5、Docker安装所需镜像服务，如MySQL、Redis、Nginx等

## 项目配置

### 日志

1、日志：必须分类明确，如下

- 业务异常 WARN

- 系统异常 ERROR

- 调试信息  DEBUG

2、日志大小、有效期必须设置合理，防止磁盘空间不足。

3、日志配置遵循JSON格式，包含时间戳、线程名、TraceId等，便于ELK收集分析。

### 配置文件

配置文件中不允许硬编码，隐私信息如IP、密码、密钥等信息必须配置在环境变量中。



1、安装Milvus

先下载好启动文件

```powershell
https://github.com/milvus-io/milvus/releases/download/v2.4.1/milvus-standalone-docker-compose.yml
```

配置镜像加速器

```powershell
sudo nano /etc/docker/daemon.json

{
  "registry-mirrors": [
    "https://mirror.ccs.tencentyun.com"
  ]
}

sudo systemctl daemon-reload
sudo systemctl restart docker
```

2、启动Milvus

```powershell
sudo docker compose up -d
```

```powershell
cd ~
mkdir milvus-compose && cd milvus-compose
```

创建一个名为docker-compose.yml的文件

```powershell
version: '3.5'

services:
  etcd:
    container_name: milvus-etcd
    image: quay.io/coreos/etcd:v3.5.5 # 你本地已有的版本
    environment:
      - ETCD_AUTO_COMPACTION_MODE=revision
      - ETCD_AUTO_COMPACTION_RETENTION=1000
      - ETCD_QUOTA_BACKEND_BYTES=4294967296
    volumes:
      - ./etcd:/etcd
    command: etcd -advertise-client-urls=http://127.0.0.1:2379 -listen-client-urls http://0.0.0.0:2379 --data-dir /etcd
    healthcheck:
      test: ["CMD", "etcdctl", "endpoint", "health"]
      interval: 30s
      timeout: 20s
      retries: 3

  minio:
    container_name: milvus-minio
    image: minio/minio:RELEASE.2023-03-20T20-16-18Z # 你本地已有的版本
    environment:
      MINIO_ACCESS_KEY: minioadmin
      MINIO_SECRET_KEY: minioadmin
    volumes:
      - ./minio:/minio_data
    command: minio server /minio_data --console-address ":9001"
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:9000/minio/health/live"]
      interval: 30s
      timeout: 20s
      retries: 3

  milvus-standalone:
    container_name: milvus-standalone
    image: milvusdb/milvus:v2.4.1 # 你本地已有的版本
    command: ["milvus", "run", "standalone"]
    environment:
      ETCD_ENDPOINTS: etcd:2379
      MINIO_ADDRESS: minio:9000
    volumes:
      - ./milvus:/var/lib/milvus
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:9091/healthz"]
      interval: 30s
      start_period: 90s
      timeout: 20s
      retries: 3
    ports:
      - "19530:19530" # 这是你的应用连接的关键端口
      - "9091:9091"   # Milvus Dashboard 端口
    depends_on:
      - etcd
      - minio
```

```powershell
docker-compose up -d
```



3、上传Python项目并配置

```powershell
# 创建虚拟环境
python3.12 -m venv .venv

# 激活虚拟环境
source .venv/bin/activate

# 升级 pip
pip install --upgrade pip

# 安装依赖
pip install -r requirements.txt

# 验证安装
pip list | grep -E "fastapi|langgraph|pymilvus|dashscope"
```

测试专用

```pow
# 1. 激活虚拟环境
source .venv/bin/activate

# 2. 安装 setuptools
pip install setuptools

# 3. 升级 pymilvus
pip install --force-reinstall \
    "langchain==0.1.9" \
    "langchain-community==0.0.24" \
    "langchain-core>=0.1.30,<0.2.0" \
    "langsmith>=0.1.0,<0.2.0" \
    "langgraph==0.0.26" \
    "dashscope==1.14.4" \
    "pymilvus==2.3.6"

# 4. 验证版本
pip list | grep -E "langchain|langgraph|langsmith"


# 5. 启动应用
nohup .venv/bin/python -m app.main > app.log 2>&1 &
```

4、环境变量设置

```powershell
export ALIYUN_ACCESS_KEY_ID=xxx
export ALIYUN_ACCESS_KEY_SECRET=xxx
export JWT_ADMIN_SECRET_KEY=smartLearningAcademyAdminSecretKey2026Secure
export JWT_USER_SECRET_KEY=smartLearningAcademyUserSecretKey2026Secure
export MYSQL_HOST=localhost
export MYSQL_PASSWORD=root
export MYSQL_USERNAME=root
export REDIS_HOST=localhost
export REDIS_PASSWORD=123456
export SMS_SIGN_NAME=速通互联验证码
export SMS_TEMPLATE_CODE=100001
export TENCENT_COS_SECRET_ID=xxx
export TENCENT_COS_SECRET_KEY=xxx
export MCP_SERVICE_TOKEN=xxx
export DASHSCOPE_API_KEY=xxx
```

5、启动Java项目

```powershell
nohup java -jar xxx.jar   > app.log 2>&1 &
```

6、构建前端项目并配置Nginx

```powershell
npm install 
npm run build
```



